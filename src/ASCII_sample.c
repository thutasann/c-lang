#include <stdio.h>

/**
    ## ASCII sample in C
 */
void ASCII_sample(void) {
    printf("\n------>> ASCII sample in C\n");
    const char ch = 'a';
    printf("ASCII value of %c is %d\n", ch, (int)ch); // Prints: ASCII value of a is 97
}